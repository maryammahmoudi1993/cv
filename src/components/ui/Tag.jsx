const Tag = ({ children }) => (
  <li className="px-3 py-1.5 rounded-full border border-white/10 bg-white/[0.04] font-mono text-xs text-ink-tag list-none">
    {children}
  </li>
);

export default Tag;
